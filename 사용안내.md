# CHO 국립중앙박물관 보물찾기

4개 조에 각 30개씩 배정한 모바일 웹페이지입니다. 전체 유물은 48종이며, 각 조 안에는 중복이 없고 조 사이에는 중복이 있습니다. 원본 HTML의 조별 입장·검색·관람 체크·도록 흐름을 새로 구현했습니다.

## 바로 열기

압축을 풀고 `index.html`을 브라우저로 여세요. 사진 48장과 데이터가 폴더에 포함되어 있어 API 연결 없이 미션을 사용할 수 있습니다. `assets` 폴더, `data.js`, `app.js`, `styles.css`를 HTML과 함께 보관하세요. 로컬 파일 저장이 제한되는 브라우저에서는 아래 로컬 서버 실행 방식을 이용하세요.

Node.js 22.19 이상에서는 `실행.cmd`를 열고 `http://127.0.0.1:8787`로 접속할 수 있습니다. 서버를 종료하려면 열린 터미널에서 Ctrl+C를 누르세요. 서버는 기본적으로 이 컴퓨터에서만 접속할 수 있습니다. 휴대폰에 공유하려면 정적 웹 호스팅에 HTML·JS·CSS·assets를 함께 올려야 하며, 이번 작업에서는 외부 배포하지 않았습니다.

## 포함 기능

- 조별 30개 미션, 시대·지역 필터, 이름·소장품번호·전시위치 검색
- 관람 완료 체크, 진행률, 관찰 메모, 수집 도록, JSON 기록 내려받기
- 현재 필터에 표시된 미션 인쇄
- 운영자 전시 확인표 및 JSON 내보내기
- 조별 배정표 CSV, 원본 49행 대조표 CSV

기록은 **기기·브라우저·접속 주소별**로 저장됩니다. 대표 기기 한 대를 정해 사용하세요. 같은 조의 다른 휴대폰과 자동 동기화되지 않습니다. 파일 방식에서 서버 방식으로 접속 주소를 바꾸면 저장 공간도 달라집니다. 기록 파일의 다시 가져오기 기능은 이번 버전에 포함되지 않습니다.

## 조별 구성

|분류|조마다|
|---|---:|
|선사·초기철기|4|
|삼국·가야|7|
|통일신라|3|
|고려|5|
|조선|7|
|근대·대한제국|1|
|세계|3|
|합계|30|

모든 조가 고구려·백제·신라·가야를 포함합니다. 근현대 범주 중 이번 목록에서는 대한제국 인장 1개를 근대 미션으로 반영했습니다. 현대 유물은 별도로 넣지 않았습니다. 1조는 선사, 2조는 조선, 3조는 세계, 4조는 고려부터 보는 **분류 순서 제안**입니다. 전시실을 연결한 최단거리 경로는 아닙니다.

운영 제안: 안내 10분, 관람 90분, 조별 발표 15분, 정리 5분. 관람 완료 유물당 1점으로 최대 30점. 실제 동선과 혼잡도를 확인해 시간을 조정하세요.

## 유물·사진 대조 결과

2026-09-09에 국립중앙박물관 공식 소장품 상세페이지의 동일 레코드에서 유물명·소장품번호·사진을 함께 수집했습니다. 검색 결과 첫 사진을 이름만 보고 붙이지 않았습니다. 대표 사진은 그 레코드의 ‘이미지 1’ 또는 이에 대응하는 공식 썸네일입니다. 원본 URL과 이미지 URL은 `catalog.json` 및 배정표에 남아 있습니다. 이미지 48개가 실제 이미지 응답을 반환하는 것을 확인했습니다.

CSV의 49행은 다음과 같이 반영했습니다.

- 16·24행: 동일한 분청사기 상감 구름 용무늬 항아리(덕수2411)로 병합.
- 18·39행: 동일한 백자 달항아리(접수702)로 병합.
- 21·34행: 동일한 청동 은입사 물가풍경무늬 정병(본관2426)으로 병합.
- 3행 ‘대한제국 황제의 인장’ 링크는 5행의 감산사 아미타불이었습니다. 황제 인장 주제를 살리기 위해 국새 칙명지보(신수241)를 선정했습니다. **원본이 정확히 이 인장을 의도했는지는 운영자 확인이 필요합니다.**
- 백자 병(덕수3898)은 중국 송, 십이지상(말)(덕수5198)은 고려로 분류했습니다.
- 금관(본관9435)은 금관총 금관입니다. 황남대총 금관과 같지 않습니다.
- 반가사유상 덕수3200, 신수4165는 서로 다른 유물이며 각각 구분했습니다. 사유의 방의 특정 전시품이라고 단정하지 않았습니다.
- 대동여지도(건희3946)는 공식 제작시대가 시대미상입니다. 이 소장본의 제작시대를 조선이라고 확정하지 않고, 조선 지도 주제로 배정했습니다.
- 태자사 낭공대사비는 [공식 큐레이터 설명](https://www.museum.go.kr/MUSEUM/contents/M0501000000.do?relicRecommendId=16884&schM=view)의 고려 954년을 반영했습니다.
- 고구려 연꽃 짐승얼굴무늬 수막새(증1623), 가야 사슴뿔모양 잔(신수6700)을 시대 보완용으로 추가했습니다.

그 결과 CSV 반영 유물 46종 + 보완 유물 2종 = 48종을 모두 한 조 이상에 배정했습니다.

## 행사일 전시 확인

**사진 매칭 완료가 행사일 전시 확정을 뜻하지는 않습니다.** 공식 페이지에 전시위치가 기재된 유물은 23종, 미기재는 25종입니다. 위치 미기재를 ‘비전시’로 판단하지 않았고 임의의 전시실·진열장 번호를 만들지 않았습니다. 회화·서적과 대여 가능한 유물은 전시 교체 여부를 확인하세요.

`organizer.html`에서 행사일 확인과 실제 위치를 기록할 수 있습니다. 이 기록은 운영자 브라우저에만 저장되고 참가자용 위치 데이터를 자동 변경하지 않습니다. 변경 사항은 행사 전에 `data.js`의 해당 유물과 팀 배정에 반영해야 합니다. 교체할 때는 같은 카테고리, 조 안에 중복 없음, 30개 유지 조건을 지키세요.

## API 상태와 설정

실제 전송한 API 요청은 `/list` 1회입니다. 첫 응답에서 HTTP 429와 코드 22, `LIMITED_NUMBER_OF_SERVICE_REQUESTS_EXCEEDS_ERROR`가 반환되어 이후 대량 호출하지 않았습니다. 승인 직후 첫 호출이 제한된 이유는 이 응답만으로 확인할 수 없습니다. 호출량·승인·한도 설정은 계정의 활용신청 화면과 제공기관 확인이 필요합니다. 새 인증키 발급으로 호출량이 초기화된다고 가정하지 마세요.

[공공데이터포털 공식 오류 안내](https://www.data.go.kr/tcs/dss/selectErrCodePopupView.do)에서 코드 22는 일일 호출 허용량 초과, 30은 미등록 인증키로 구분합니다. 코드 22의 공식 조치는 호출량 초기화 후 이용 또는 트래픽 증설입니다.

API 한도 복구 후 PowerShell에서 다음처럼 환경변수를 설정한 뒤 서버를 시작하세요. 아래 예시의 자리표시자에 발급받은 키를 넣습니다. Encoding/Decoding 키 모두 입력 가능하며 서버가 인코딩된 키를 한 번 디코딩한 후 URLSearchParams로 한 번 인코딩합니다.

```powershell
$env:EMUSEUM_SERVICE_KEY = '발급받은_인증키'
node --use-system-ca server.mjs
```

실제 인증키는 배포 파일에 포함하지 않았습니다. `.env.example`은 형식 참고용이며 자동으로 읽지 않습니다. API 서버는 검색을 국립중앙박물관 기관 코드 `PS01001001`로 제한하고, 성공 응답은 24시간 메모리 캐시합니다. 실제 상위 API 호출 간격은 최소 1초이고, 일일 한도 초과를 받으면 해당 실행 중 추가 호출을 차단합니다. 한도 복구 확인 후 재시작하세요.

서버 경로:

- `/api/emuseum/list?name=검색어`: 국립중앙박물관 목록 20건
- `/api/emuseum/detail?id=목록에서_반환된_ID`: 실제 고유 ID로 상세 조회
- `/api/emuseum/code?parentCode=PS01`: 공식 코드 목록 조회

공식 API의 `PS...` ID와 박물관 웹사이트의 숫자 `relicId`는 서로 다릅니다. **숫자 relicId를 API ID로 바꾸거나 추정하지 않습니다.** 이번 카탈로그의 `apiId`는 실제 검증 전이므로 null입니다. 운영자 화면에서 실제 목록 ID로 상세 대조할 수 있으나 미션의 사진·이름을 자동 교체하지 않습니다. API의 정상 XML 응답과 실제 ID 대조는 한도 문제로 실환경 검증을 완료하지 못했습니다.

명세 출처: [e뮤지엄 Open API 안내](https://www.emuseum.go.kr/openApi), [해당 공공데이터 상세](https://www.data.go.kr/data/15159017/openapi.do).

## 저작물 출처

이 배포본의 사진은 국립중앙박물관 소장품 페이지에서 공공누리 제1유형(출처표시)을 확인한 사진입니다. API 데이터의 제0유형 표기와 사진의 개별 이용조건을 구분했습니다. 이미지 편집 없이 공식 썸네일을 보관하며, 페이지와 각 유물 상세에 출처를 표시했습니다. 관찰 미션 문구는 워크샵용으로 새로 작성했습니다.
## 추가 기능 메모

- 각 조 페이지의 `못 찾은 보물이 있다면` 영역에서 마음에 드는 유물을 검색해 도록에 추가할 수 있습니다.
- `PDF 도록 만들기`를 누르면 인쇄 창이 열립니다. 대상에서 `PDF로 저장`을 선택하면 한 페이지당 5개 유물 구성의 도록으로 저장할 수 있습니다.
- `outputs/museum-treasure` 폴더는 정적 사이트로 배포할 수 있습니다. Vercel CLI에 로그인된 상태라면 `vercel --prod` 명령으로 배포합니다. 현재 이 PC에는 Vercel CLI가 설치되어 있지 않습니다.
